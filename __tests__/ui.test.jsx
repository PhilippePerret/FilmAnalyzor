'use strict'

/**
  Pour tester que l'interface est conforme à ses attentes
**/
describe("L'interface de FilmAnalyzor", () => {
  it("contient un div principal whole-ui pour l'interface fixe", () => {
    expect(window.document.getElementById('whole-ui')).not.toBeNull()
  })
  it("contient une bande supérieure ()", () => {

  })
})
